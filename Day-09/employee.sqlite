CREATE DATABASE CompanyDB;
USE CompanyDB;

CREATE TABLE Employee (
    emp_id INT PRIMARY KEY,
    emp_name VARCHAR(50),
    department VARCHAR(30),
    salary INT
);

INSERT INTO Employee VALUES
(101, 'Rahul', 'HR', 25000),
(102, 'Priya', 'IT', 40000),
(103, 'Karan', 'Finance', 35000),
(104, 'Neha', 'Marketing', 28000);

SELECT * FROM Employee;

SELECT * FROM Employee
WHERE salary > 30000;
